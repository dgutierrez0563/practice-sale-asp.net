@model IEnumerable<LenguajesAvanzados.Models.Vendedor>

@{
    ViewBag.Title = "Vendedores";
}

<div class="panel panel-info">
    <div class="panel-heading" style="padding-top:25px;padding-bottom:25px;">
        <h1 class="panel-title text-center" style="font-size:20px;">Manejo de Vendedores</h1>
    </div>
</div>

<div class="panel panel-primary" style="margin-top:5px;">
    <div class="panel-heading" style="padding-top:20px;padding-bottom:20px;">
        <h2 class="panel-title text-center">Lista de Vendedores</h2>
    </div>
    <div class="panel-body">
        <table class="table table-striped table-hover">
            <tr class="info">
                <th>
                    @Html.DisplayNameFor(model => model.Name)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Apellido)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.FechaCreado)
                </th>
                <th style="text-align:center">Acciones</th>
            </tr>

            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Name)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Apellido)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.FechaCreado)
                    </td>
                    <td>
                        <ul class="btn-group text-center">
                            <li class="btn btn-warning btn-xs">@Html.ActionLink("Edit", "Edit", new { id = item.IDVendedor })</li>
                            <li class="btn btn-info btn-xs">@Html.ActionLink("Details", "Details", new { id = item.IDVendedor })</li>
                            <li class="btn btn-danger btn-xs">@Html.ActionLink("Delete", "Delete", new { id = item.IDVendedor })</li>
                        </ul>
                    </td>
                </tr>
            }
        </table>
        <br />
        <a class ="btn btn-primary" style="font-size:16px;" @Html.ActionLink("Create New", "Create")
        </a>
    </div>
</div>

